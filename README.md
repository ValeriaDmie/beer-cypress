# beer-cypress
Cypress E2E tests for Beer app

## Starting guide


```shell
npm install
```

Open Cypress UI:
```shell
npm run cypress:open
```

Run all commands from command line:
```shell
npm run cypress:run
```